{
  "routes": [
    {
      "method": "GET",
      "path": "/cuentas",
      "handler": "cuentas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cuentas/count",
      "handler": "cuentas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cuentas/:id",
      "handler": "cuentas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cuentas/:id/exist",
      "handler": "cuentas.exist",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cuentas",
      "handler": "cuentas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cuentas/:id",
      "handler": "cuentas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cuentas/:id",
      "handler": "cuentas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
